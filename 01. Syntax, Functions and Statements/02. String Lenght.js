function solve(first,second,third){
    let totalLenght = first.length+second.length+third.length;
    console.log(totalLenght);

    let averageLenght=Math.floor(totalLenght/3);
    console.log(averageLenght);
}